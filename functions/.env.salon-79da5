# Non-secret params for the salon-79da5 project (see functions/src/config.ts). No secrets here —
# those live in Secret Manager via defineSecret (functions:secrets:set), never in a .env file.
# Working Firebase Hosting origin (razorpayOAuthCallback redirects here). chairly.app is the intended
# custom domain but isn't connected/verified on Firebase Hosting yet, so it doesn't resolve — switch
# this once it is, and redeploy functions (`firebase deploy --only functions`).
PUBLIC_BASE_URL=https://salon-79da5.web.app
